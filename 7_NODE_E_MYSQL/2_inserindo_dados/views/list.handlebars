<h1>Lista de Livros</h1>
<p>Aqui aparecerão todos os livros cadastrados no banco de dados.</p>

<!-- TODO: quando passar { books: results } pela rota, renderize a lista assim:
{{#each books}}
<div>
    <span>{{this.tittle}}</span>
    <span>{{this.pageqty}} páginas</span>
    <a href="/books/edit/{{this.id}}">Editar</a>
    <form action="/books/delete" method="POST">
        <input type="hidden" name="id" value="{{this.id}}">
        <input type="submit" value="Deletar">
    </form>
</div>
{{/each}}
-->
